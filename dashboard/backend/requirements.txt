# Athena Dashboard Backend Dependencies

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# CORS and WebSocket support
python-multipart==0.0.9
websockets==12.0

# Data validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Athena is installed from parent directory (pip install -e ../../)
# No need to list athena dependencies here - they come from athena itself
