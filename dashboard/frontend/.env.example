# Athena Dashboard Frontend Environment Variables

# API Configuration
# In development, Next.js proxy handles forwarding to backend
# In production, this should point to your API URL
NEXT_PUBLIC_API_URL=http://localhost:3000

# Node Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# Optional: WebSocket URL (if different from API)
# NEXT_PUBLIC_WS_URL=ws://localhost:8000
