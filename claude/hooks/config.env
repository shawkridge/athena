# Athena Configuration for Hooks (Local-First)
#
# IMPORTANT: Athena now runs locally via direct Python API (not HTTP).
# No configuration needed - hooks will automatically import and use the
# Athena MemoryAPI from your installed athena package.
#
# If athena is not in your Python path, update PYTHONPATH:
# export PYTHONPATH="/path/to/athena/src:$PYTHONPATH"
#
# Optional: Database path (if using non-default location)
# export ATHENA_DB_PATH="~/.claude/memory.db"
#
# Enable debug logging for hooks
export ATHENA_DEBUG=0
